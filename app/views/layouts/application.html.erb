<!DOCTYPE html>
<html>
  <head>
    <title>SaunaCompass</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="bg-blue-500 text-white">
      <div class="w-full">
        <div class="flex left-block">
          <a href="#" class="text-2xl font-bold">SaunaCompass</a>
        </div>
        <div class="flex">
          <% if customer_signed_in? %>
            <h1>Customer</h1>
            <a href="<%= destroy_customer_session_path %>" data-method="delete">ログアウト</a>
          <% elsif admin_signed_in? %>
            <h1>Admin</h1>
            <a href="<%= destroy_admin_session_path %>" data-method="delete" class="block">ログアウト</a>
          <% else %>
            <div class="right-block">
              <a href="<%= new_customer_registration_path %>">新規登録</a>
              <a href="<%= new_customer_session_path %>">ログイン</a>
            </div>
          <% end %>
        </div>
      </div>
      <style>
         .left-block {
          margin-right: auto;
          padding: 30px 120px;
        }

        .right-block {
          margin-left: auto;
          padding: 30px 120px;
        }
      </style>
    </header>

    <div class="flex justify-center">
      <%= yield %>
    </div>
  </body>
</html>
